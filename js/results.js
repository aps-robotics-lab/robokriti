// Public results renderer. Results are published by the admin dashboard to /results.
(async()=>{const wrap=document.querySelector('[data-results]');if(!wrap)return;const db=window.RoboFirebase?.db;if(!db){wrap.innerHTML='<div class="notice">Firebase is not configured.</div>';return}try{const snap=await db.ref('results').orderByChild('rank').once('value');const rows=[];snap.forEach(s=>{const x=s.val();if(x.published!==false)rows.push(x)});rows.sort((a,b)=>(a.rank||999)-(b.rank||999));wrap.innerHTML=rows.length?`<div class="data-table-wrap"><table class="data-table"><thead><tr><th>Rank</th><th>Team</th><th>Event</th><th>Score</th></tr></thead><tbody>${rows.map(x=>`<tr><td>#${x.rank}</td><td>${x.team}</td><td>${x.event}</td><td>${x.score||'—'}</td></tr>`).join('')}</tbody></table></div>`:'<div class="notice">Results will appear here once published by the organizing team.</div>'}catch(e){console.error(e);wrap.innerHTML='<div class="notice error">Results could not be loaded.</div>'}})();