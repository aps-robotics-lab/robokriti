import{init}from'./main.js';import{db,ref,onValue}from'./firebase.js';await init();const el=document.getElementById('announcementFeed');onValue(ref(db,'announcements'),s=>{const d=s.val()||{};const a=Object.values(d).sort((x,y)=>(y.createdAt||0)-(x.createdAt||0));el.innerHTML=a.length?a.map(x=>`<article class="feedItem"><span class="date">${x.date||'UPDATE'}</span><div><h3>${safe(x.title||'Announcement')}</h3><p>${safe(x.body||'')}</p></div><span class="mono">${safe(x.type||'INFO')}</span></article>`).join(''):'<div class="status">NO ANNOUNCEMENTS YET</div>'});function safe(x){return String(x).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]) )}
