import{init,cards}from'./main.js';await init();cards('homeEvents');const loader=document.getElementById('loader'),bar=document.getElementById('loaderBar'),pct=document.getElementById('loaderPct'),canvas=document.getElementById('heroCanvas'),ctx=canvas.getContext('2d',{alpha:false}),total=300,frames=[];let loaded=0,current=0;function resize(){const d=Math.min(devicePixelRatio||1,2);canvas.width=innerWidth*d;canvas.height=innerHeight*d;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(d,0,0,d,0,0);draw(current)}function draw(i){const im=frames[i];if(!im)return;const s=Math.max(innerWidth/im.naturalWidth,innerHeight/im.naturalHeight),w=im.naturalWidth*s,h=im.naturalHeight*s;ctx.fillStyle='#000';ctx.fillRect(0,0,innerWidth,innerHeight);ctx.drawImage(im,(innerWidth-w)/2,(innerHeight-h)/2,w,h)}addEventListener('resize',resize);resize();let next=0;async function load(){const workers=Math.min(8,navigator.hardwareConcurrency||4);async function worker(){while(next<total){const i=next++;const im=new Image();im.src=`assets/frames/frame_${String(i).padStart(4,'0')}.webp`;try{await im.decode()}catch{}frames[i]=im;loaded++;let v=Math.round(loaded/total*100);pct.textContent=v+'%';bar.style.width=v+'%'}}await Promise.all(Array.from({length:workers},worker));loader.classList.add('hide');render()}function render(){const r=document.getElementById('hero').getBoundingClientRect(),max=Math.max(1,document.getElementById('hero').offsetHeight-innerHeight),p=Math.min(1,Math.max(0,-r.top/max));current=Math.floor(p*(total-1));draw(current)}addEventListener('scroll',()=>requestAnimationFrame(render),{passive:true});load();
